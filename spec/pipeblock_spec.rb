require 'spec_helper'

describe Pipeblock do
  it 'has a version number' do
    expect(Pipeblock::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
